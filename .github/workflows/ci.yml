name: build pipline

on: workflow_dispatch

jobs:
  build:
    name: 'App build'
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v5.0.0

    - name: Setup Node.js environment
      uses: actions/setup-node@v5.0.0
      with:
        node-version: 18.x
        

    - run: npm install
    - run: ././scripts/deploy-for-production.sh
      

        

    
